# pkg (pacman wrapper)

I can never remember pacman's flags, and I don't always want to install a proper pacman wrapper like pamac. So this is a basic shell script wrapper for the common use-cases. It's designed to feel familiar to users of Debian's `apt` and FreeBSD's `pkg`.

See [the shell script](./pkg) for usage info.
